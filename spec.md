# Specification

## Summary
**Goal:** Implement staff authentication system with shared credentials and appointment viewing dashboard.

**Planned changes:**
- Create staff login page with username and password fields
- Add backend authentication endpoint with hardcoded credentials
- Build appointments dashboard accessible only to authenticated staff
- Implement session management to maintain login state across navigation

**User-visible outcome:** Staff members can log in using shared credentials to access a dashboard displaying all patient appointments.
